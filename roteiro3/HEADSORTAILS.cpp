#include <bits/stdc++.h>

using namespace std;

#define _                                                                      \
    ios_base < : sync_with_stdio(false);                                       \
    cin.tie(0);                                                                \
    cout.tie(0);

#define endl '\n'

int main(void) {
    short x, y, a, b;
    cin >> x >> y >> a >> b;
    
    return 0;
}